#!/usr/bin/env bash
# Installs Nginx and configures it to return "Hello World!" at the root URL

apt-get update -y -qq
apt-get install -y nginx
service nginx start
echo -n 'Hello World!' > /var/www/html/index.html
